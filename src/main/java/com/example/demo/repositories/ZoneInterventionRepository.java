/*package com.example.demo.repositories;

import com.example.demo.entities.ZoneIntervention;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;
import java.util.Optional;

@Repository
public interface ZoneInterventionRepository  extends JpaRepository<ZoneIntervention, Long>  {
    Optional<ZoneIntervention> findById (Long id);
    Optional<ZoneIntervention> findZoneInterventionByCode(Long code);
}
*/